# Kirole 硬件陪伴

这个领域描述 App 如何为单台墨水屏设备准备内容，以及设备在离线时如何继续响应用户操作。

## 任务内容

**设备任务库**：
设备离线进入任务时所需的**当天**未完成任务内容集合（2026-08-04 修订）：截止日期在当天的任务，加上用户手动设为今天的无日期任务。未来、过期和未选入的无日期任务不属于设备任务库；任务不按屏幕一次能够显示的条数截断；完成或删除后不再属于设备任务库；跨自然日后按新日期重新计算。
_避免：全量任务库、可见任务集、Top Tasks_

**任务库版本**：
设备在某一时刻可完整使用的一份任务库内容。新版本在任务数据和配套文字全部准备好后一次生效，准备期间旧版本继续有效。
_避免：半成品任务包、局部任务版本_

**已提交版本**：
设备已经完整接收并能够使用的最近一份任务库版本。失败或只收到一部分的新版本不会替换它；没有已提交版本的新设备才显示空任务列表。
_避免：最新发送版本、半包版本_

**待发送版本**：
已经完成三分钟稳定窗和文字准备，但尚未成为设备已提交版本的最新完整内容。新变化可以替换它，旧的中间版本不再补发。
_避免：失败半包、待续传分片_

**任务库更新**：
在已有任务库基础上提交的一组任务新增、修改和删除记录。首次同步或设备任务库丢失时发送当天完整任务库，平时只发送变化记录。
_避免：每次重传完整任务库、半包续传_

**即时任务动作**：
用户完成或跳过任务的明确操作。动作立即生效并返回最新任务状态，不进入三分钟稳定窗。
_避免：任务内容编辑、延迟任务动作_

**离线任务动作**：
蓝牙断开期间，用户在设备上完成或跳过任务的操作。设备立即在本地执行、更新顺序任务队列并返回来源页面，同时保存操作；重连后 App 先接收这些操作，不用旧状态把设备改回去。
_避免：离线待确认动作、重连覆盖、无效按键_

**完成任务**：
用户在任务专注页短按，结束当前专注并把任务标为完成。任务从设备任务库移除，下一项成为顺序任务队列的新队首，设备返回进入专注前的来源页面。
_避免：跳过任务、删除任务、固定返回概览_

**完成优先**：
同一次断联期间，设备记录的完成动作与 App 或外部服务对同一任务的普通内容修改发生冲突时，以完成状态为准。任务保持完成并从设备任务库移除，内容修改不能让它重新变为未完成。
_避免：编辑复活任务、旧状态覆盖完成_

**跳过任务**：
用户在任务专注页长按，结束当前专注但不完成或删除任务。任务移动到顺序任务队列末尾，设备返回进入专注前的来源页面。
_避免：完成任务、删除任务、取消任务_

**跳过合并**：
同一次断联期间，设备记录的跳过动作与 App 或外部服务对同一任务的内容修改同时发生时，两边结果都保留。任务继续未完成并位于队尾，内容采用最新版本，受影响的任务阶段文案重新准备。
_避免：跳过丢失修改、修改取消跳过、任务完成_

**删除优先**：
同一次断联期间，设备记录的跳过动作与 App 或外部服务删除同一任务发生冲突时，以删除为准。任务从设备任务库移除，队尾位置变化不再应用。
_避免：跳过恢复删除任务、保留无对象位置_

**顺序任务队列**：
设备任务库供用户依次进入的有序视图。短按只进入队首任务，完成队首后下一项成为新的队首，跳过队首则把它移到队尾；设备不提供任意任务选择。
_避免：滚轮选择、任意跳转、任务网格_

**队列排序合并**：
断联期间设备发生跳过动作，同时 App 调整任务顺序时的排序规则。先采用 App 最新顺序，再把断联期间跳过的任务按实际跳过先后移到队尾。
_避免：整队覆盖跳过、跳过覆盖 App 排序、随机合并_

**三分钟稳定窗**：
任务内容或当天日程最后一次发生变化后，等待连续三分钟没有新变化的时间段。相关文字在窗内准备；再次变化会重新计时，到点只同步最新的完整内容；即时任务动作不进入这个时间段。稳定窗不因蓝牙断开或重连而重新计时：重连时已经结束就立即发送，尚未结束就只等待剩余时间。
_避免：立即推送、固定周期同步、重连重新计时_

**任务阶段文案**：
与单个任务预先匹配的三条陪伴文字，分别用于进入状态、逐渐投入和深度专注。设备按本地专注时长选择，不在阶段切换时向 App 索取新文字。
_避免：现场生成文案、动态请求文案_

**文案准备失败**：
同一批任务阶段文案首次生成和后台追加的一次重试都失败，或到三分钟稳定窗结束仍未完成。达到这个条件后，使用本地阶段模板补齐，迟到结果不再改变已经发送的版本。
_避免：首次请求失败、暂时失败_

## 伴侣文字

**整套预生成对话包**：
设备可能显示的全部预生成伴侣文字的统称，不是一次蓝牙传输：逐任务三阶段文字随设备任务库保存；每日时段、逐日程、屏保和总结文字随当天内容包保存。设备按本地时间、任务、日程和页面状态自行选择，不在显示现场向 App 索取新文字。
_避免：当前一句、现场对话、动态对话请求_

**当天文案准备失败**：
新自然日的当天文案首次生成失败，且后台追加的一次重试仍然失败。达到这个条件后，App 用当天内容的本地模板补齐，并立即发送完整的当天内容包。
_避免：首次请求失败、无限重试、保持空内容_

**当天内容包**：
当天日程快照与每日时段、逐日程、屏保和总结文字组成的设备内容集合，不包含逐任务三阶段文字。它与设备任务库分别保存版本，日程或每日文字变化不要求重传当天完整任务库。
_避免：完整任务库、跨日内容包_

**跨日失效**：
设备本地日期变化后，上一自然日的日程和每日文字立即停止使用，显示新日期、空日程和本地默认对话，直到收到新一天的当天内容包。任务库由 App 立即按新的一天重新计算并发送（昨天的任务经变化记录移除）；设备在新版本到达前继续使用旧任务库，不自行清空。跨日更新不进入三分钟稳定窗；蓝牙已连接时，新一天内容准备完整后立即发送。
_避免：沿用昨日内容、跨日保留日程、设备自行清空或过滤任务库、跨日等待稳定窗_

**受影响文案**：
因为来源内容或角色口吻发生变化而需要重新准备的文字。无关任务或日程的变化不会让已经匹配的文案失效。
_避免：全量重写、无条件重新生成_

## 日程内容

**当天日程快照**：
设备当前自然日内的全部日历日程及其配套文字集合。未来日期的日程不属于当天日程快照。
_避免：完整日程库、未来日程包_

## 设备页面

**来源页面**：
用户进入临时页面前所在的设备页面。退出临时页面时返回该页面，而不是跳到另一个固定页面。
_避免：固定返回页、默认首页_

**断联专注**：
用户专注期间蓝牙断开后仍然有效的同一次专注。设备继续按本地时钟计时并保持当前专注页，App 不结束专注；重连后设备不切换页面，只核对当前任务和已专注时长。
_避免：断联即结束、重连回概览、重连新建专注_

**专注任务编辑锁**：
设备通过蓝牙连接且用户正在专注时，App 不允许修改当前专注任务。蓝牙断开后才允许修改；重连同步的新内容不替换正在显示的专注页，退出后再次进入任务时使用最新版。
_避免：联网专注中编辑、专注中途换文案、重连强制刷新_

**专注中删除**：
设备断联专注的任务同时被 App 或外部服务删除时，重连不打断当前专注，也不切换页面。用户主动完成或跳过后，任务彻底移出设备任务库，不再进入顺序任务队列。
_避免：重连强制退出、删除任务重新入队、立即切页_

**已删除任务专注结算**：
设备继续专注一个已被 App 或外部服务删除的任务，并由用户短按完成时，本次专注时长照常记录并计算能量奖励；任务保持删除，不会因此恢复或重新进入设备任务库。
_避免：删除抹去专注、完成恢复任务、任务重新入队_

**设备静态反馈**：
墨水屏设备以静态画面表达状态变化，不提供声音、震动或动画。iPhone App 的声音、触觉反馈和动画不属于这个概念。
_避免：设备动画、硬件触觉反馈、全产品静音_
