# Outlook 真机验收记录

状态：部分完成。下列结果仅证明已执行的场景，不代表真机验收全部通过或可以发布。

本文前半部分保留 2026-09-06 的历史快照；最新进展见末尾「2026-09-07：663 内部版补充验收」。旧版工具限制和未完成清单不应直接当作当前状态。

- 日期：2026-09-06
- 源码：eda2117，分支 feat/outlook-calendar-sync
- 设备：iPhone 12 Pro Max，iOS 26.6.1
- 已安装版本：2.0.1 (659)，Debug，Microsoft 构建开关开启
- 本地 Secrets.xcconfig 的 MICROSOFT_OAUTH_ENABLED 仍为 0

## 已观察到的结果

1. 用户已完成 Microsoft 登录。实际是否经过 Authenticator 未确认。
2. 真机保存了 Outlook 连接状态、成功同步时间和非空 delta cursor；未读取访问令牌。
3. 强退重启后，Outlook 登录与连接保持。
4. 网页默认日历与手机所用 Microsoft 账户核对一致。
5. 在网页创建临时事件 Kirole QA 0906 All Day：2026-09-06 全天，无参与人，无提醒。
6. 真机重启后成功导入该事件。手机当前 GMT+1；首页显示在 Sunday, Sep 6，标为 All Day。
7. 事件详情显示 Outlook Calendar 来源、Sunday, Sep 6、All Day。点击标题未进入编辑状态。
8. Google Calendar 显示同步错误；此前持久化的唯一 Google 事件日期为 9 月 3 日。
9. 设备区域处于寻找 Kirole 硬件状态，未取得硬件显示结果。

## 尚未完成

- 真实 Authenticator 切出与返回。
- 纽约时区首次导入全天事件。
- 已缓存全天事件切换时区后日期是否保持正确。
- Kirole 整体登出、强退重启、重新登录后 Outlook 不自动恢复。
- Outlook 单独断开后重启，且不影响 Google。
- Google 与 Outlook 的真实事件混排，以及超过 8 行时的硬件显示。

## 源码检查发现，尚待真机复现

全天事件映射只处理 Graph 返回的变更；缓存保存的是绝对 Date。切时区后的 Adjust 仍发起普通增量同步。如果 Graph 没有重发旧事件，缓存日期可能落到前一天。不要以断开重连后的首次导入正常替代这个场景的验收。

对应源码为 `MicrosoftSyncEngine+Mapping.swift` 的 `applyOutlookDelta`、`MicrosoftSyncEngine.swift` 的增量同步流程，以及 `ContentView.swift` 中的时区调整操作。

## 当前操作限制

iPhone 镜像能够点击和截图，但自动滚动未推动 Settings 页面，已请用户手动滚到 Account / Sign Out 区域。Chrome 连接器随后提示需更新扩展。没有修改手机时区，没有执行登出或断开，也没有修改源代码。

临时全天事件保留在 Outlook 默认日历中，供后续验收；本轮未创建其他日历事件。

## 2026-09-07：663 内部版补充验收

环境：同一台 iPhone 12 Pro Max / iOS 26.6.1，已安装 2.0.2 (663)，源码 `5b3c00a`。InternalRelease 的 `MICROSOFT_OAUTH_ENABLED = 1`；硬件固件 0.1.16。本轮直接控制 iPhone 镜像并采集物理 iPhone 的 BLE 日志，没有替换安装包或修改产品代码。

已执行：

- Google Calendar、Google Tasks、Outlook Calendar、Apple Calendar、Apple Reminders 同时连接；Home 实际混排 Apple 假日、Apple 测试事件、Google 定时重复事件和 Outlook 全天测试事件。源端及 App 均有实际记录，不仅是连接开关。
- Outlook 创建 `Kirole QA 663 Outlook AllDay`，覆盖 9 月 7–8 日，无参与者、Teams 会议或提醒。App 回前台后导入，正文正确；只读编辑页无 Save、字段不可编辑，并提示去 Outlook 编辑。原始日期边界为 9 月 7 日 00:00 至 9 月 9 日 00:00。
- 源端删除 Outlook 测试事件后，App 最终同步删除。冷启动后五个来源仍保持连接。本轮只验证保留登录状态下的重启，没有执行整体登出或单独断开 Outlook。
- 硬件主动断开、扫描、重新连接后恢复 Connected，最终 Last Sync 为绿色。采集到真实 `0x25` / `0x30` 入站日志；日志只含类型和长度，不足以逐帧直接解码 RESULT，也没有直接观察硬件屏幕上的混排和容量截断。
- 本轮 QA663 事件、任务和提醒已清理；9 月 6 日保留的 `Kirole QA 0906 All Day` 未动。

新发现的确定问题：

- 跨日事件只显示开始日：上述 Outlook 事件在 Home 的 9 月 7 日出现，9 月 8 日漏显示。`DaySectionView` 只按 `startTime` 同日过滤，影响所有来源的跨日事件。**这与缓存全天事件切换时区后的日期漂移是两个独立问题，不能相互替代验收。**
- 日程详情的 Open In 在 Apple、Google、Outlook 上均无跳转；公共组件仅显示文字和图标，没有绑定操作。
- 隐私政策与天气来源的 SwiftUI Link 多次点击无跳转。Safari 可启动，切普通标签组后仍复现；根因待定位，自定义 scene 生命周期是待排查候选，尚不能认定为已确认的根因。

更新后的 Outlook 待验收项：

1. 真实 Authenticator 切出与返回。
2. 纽约时区首次导入全天事件。
3. 已缓存全天事件切换时区后日期保持正确；必须保留缓存并走增量同步，不以断开重连重新导入代替。
4. 整体登出、强退、重新登录后 Outlook 不自动恢复。
5. Outlook 单独断开后重启，且不影响 Google。
6. 超过 8 条事件时的硬件实际显示。App 的多来源混排已验证，硬件容量边界尚未验证。
7. 跨日展示和外链问题修复后的真机回归。

本机详细证据：`/Users/demon/Library/Logs/Kirole/qa/20260907-build663-full/REPORT.md`、同目录 `QA.md` 与 `ble-transcript.log`（354 行日志，不等于 354 个独立帧）。
