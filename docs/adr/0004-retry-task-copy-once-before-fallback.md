# 任务阶段文案失败后只重试一次

任务阶段文案首次生成失败后，App 在后台追加一次重试；首次生成和重试都必须在当前三分钟稳定窗内结束。两次都失败，或稳定窗到点仍没有最新结果，就使用对应角色和阶段的本地固定模板补齐，迟到结果直接丢弃。设备在准备期间继续使用上一份完整任务库，不接收先兜底、后 AI 的两个中间版本；到点只发送一次新的完整任务库版本。单个任务的 AI 失败不能删除任务或阻止完整任务库最终发送。
