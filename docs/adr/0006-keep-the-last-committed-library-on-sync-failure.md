# 同步失败时保留最后一份完整任务库

新的任务库版本只有被设备完整接收后才能替换已提交版本。蓝牙中断、写入失败或只收到部分数据时，设备继续使用上一份完整任务库，日期仍由本地时钟更新；普通失败不能清空任务。只有从未成功接收过任何任务库的新设备显示空任务列表和本地默认对话，App 则保留最新待发送版本供后续重传。

跨日后 App 补发新一天任务库失败时同样适用：设备继续显示旧已提交任务库（哪怕内容是昨天的），直到重试成功——过期显示优于空白或半包。
