# Kirole 固件功能规格文档

**版本:** v1.5.5
**更新日期:** 2026-06-29
**状态:** 联调冻结版（固件功能参考）
**v1.5.5 变更:** §7.2 扩为「字库/角色映射/字号」——内置 **Lugrasimo（宠物气泡 `PetDialogue`）+ Calibri（其余所有文本）** 两套字库，按**字段语义角色**渲染，**App 不传字体**、固件按字段映射 + Lugrasimo 缺字回退 Calibri。对齐 BLE 协议 §6.6（关联版本 v2.5.12→v2.5.13）。
**v1.5.4 变更:** 补记 §3.6——屏保叠加金句的**场景底图**选择/切换从旧 `0xAA 01 01` 升级为 `0x17` SceneUnlock 业务帧（payload=1B SceneId：`0x00=harbor / 0x01=forest / 0x02=nightCity`，dev/secure 均可发；与上条 v1.5.3 屏保 `0x16` **同批迁移**，此前漏记）。固件需实现 `0x17` 解析；注意 `0x17` 出站=SceneUnlock、入站=ReminderDismissed（方向分离）。见 BLE 协议 §4.16。关联 BLE 协议版本 v2.5.10→v2.5.12。
**v1.5.3 变更:** §3.6 屏保金句帧从旧 `0xAA 01 02` 开发命令**升级为 `0x16` Screensaver 业务帧**（dev/secure 两种模式均可发，原 secure-mode 被拒的限制已解决）；固件改为实现 `0x16` 解析 + 渲染 Quote/Author，见 BLE 协议 §4.15。关联 BLE 协议版本 v2.5.9→v2.5.10。
**v1.5.2 变更:** §3.1/§3.2 由旧「混合时间线」模型更新为现行「常驻气泡 + 数据面板」（`PetDialogue` + `Events[]` + `TopTasks[]` + `SettlementData` + `DaySummary` + `FirstUp`），对齐 BLE通信协议规格文档 §4.7/§6.5（当前 v2.5.10）；旧字段 `morningGreeting/dailySummary/firstItem/currentScheduleSummary/mixedTimeline/companionPhrase` 标注废弃；关联文档 BLE 协议版本 v2.3.5→v2.5.10。
**v1.5.1 变更:** §3.6 屏保模式——金句改为 App 经 BLE 动态推送、固件叠加绘制（底图无字、断连前缓存）。
**参考产品:** Inku Daily Schedule Planner

---

## 目录

1. [产品概述](#1-产品概述)
2. [硬件规格](#2-硬件规格)
3. [显示页面设计](#3-显示页面设计)
4. [交互流程](#4-交互流程)
5. [伴侣显示系统](#5-伴侣显示系统)
6. [数据同步](#6-数据同步)
7. [4寸 vs 7.3寸差异](#7-4寸-vs-73寸差异)
8. [固件开发指南](#8-固件开发指南)

---

## 1. 产品概述

### 1.1 产品定位

**Unlock the Flow, Make it Happen.**

Kirole 是一款面向深度知识工具用户的专注力伴侣设备。它把 iOS App 中的任务、日程、智能提醒、专注状态和三位 IP 伴侣同步到 E-ink 屏幕，帮助用户在低干扰界面里看到今天要做什么、现在该关注什么。

### 1.2 核心功能

| 功能 | 描述 |
|------|------|
| 伴侣陪伴 | Joy / Silas / Nova 三位 IP 伴侣提供短句陪伴和情绪反馈 |
| 专注反馈 | 反映用户专注模式和注意力数据，量化专注时间与中断次数 |
| 智能提醒 | 基于行为模式的上下文感知提醒，在合适时机推送到设备 |
| 任务展示 | 显示今日任务列表，支持完成标记 |
| 场景解锁 | 通过专注获得能量瓶，逐步解锁 harbor / forest / nightCity |
| 日程提醒 | 展示日历事件和时间安排 |
| 每日结算 | 统计当日完成情况，含专注指标和积分奖励 |

### 1.3 产品形态

- **设备类型:** 展示型 E-ink 设备
- **尺寸规格:** 4寸 / 7.3寸 两种规格
- **交互方式:** 电源键 + 旋钮按键 + BLE 连接 iOS App
- **供电方式:** 内置电池 + USB-C 充电

### 1.4 目标用户

- 深度知识工具用户（使用多种生产力工具但执行力不足）
- 远程工作者
- 自由职业者
- 需要专注力管理的用户
- 喜欢像素风格/电子宠物的用户

---

## 2. 硬件规格

### 2.1 屏幕规格

#### 4寸屏幕

| 参数 | 规格 |
|------|------|
| 分辨率 | **552 × 768 像素**（原生竖向；**横向使用 768×552**） |
| 像素密度 | **237 DPI** |
| 显示技术 | E Ink Spectra 6, 4bpp 全彩色（6色） |
| 灰度级别 | 6 色（黑、白、黄、红、蓝、绿） |
| 帧缓冲 | 4bpp，**211,968 bytes**（768×552÷2） |
| 刷新时间 | 全刷新时间待实测（参考 7.3 寸约 12s @25C） |
| 可视角度 | 180° |

#### 7.3寸屏幕

| 参数 | 规格 |
|------|------|
| 分辨率 | **1600 × 1200 像素（4:3）**（原生竖向记 1200×1600；横向使用 1600×1200） |
| 像素密度 | **282 DPI** |
| 显示技术 | E Ink Spectra 6, 4bpp 全彩色（6色） |
| 灰度级别 | 6 色（黑、白、黄、红、蓝、绿） |
| 帧缓冲 | 4bpp，**960,000 bytes** |
| 刷新时间 | ⚠️ 文档旧值 800×480 约 12s @25C；实物 1600×1200（像素约为其 5 倍）刷新慢于此，待实测 |
| 可视角度 | 180° |

> **2026-06-26 更新（硬件确认最新尺寸）**：**4 寸 = 552×768 @ 237 DPI（横向 768×552）**、**7.3 寸 = 1600×1200 @ 282 DPI（原生 1200×1600，横向使用）**，均 4:3、横向使用，与设计 PSD（第一屏 6.psd 画布 1600×1200）一致。4 寸帧缓冲随之确定为 211,968 bytes。
> **2026-06-25 更正**：7.3 寸分辨率经硬件确认为 1600×1200（4:3）（原误为 800×480）；下文凡涉旧分辨率的帧缓冲、Flash 缓存、布局/字号/信息密度（§7）、伴侣图尺寸（§5.3）**均需按新分辨率重核**。

#### 屏幕接口（SPI 4-wire）

| 参数 | 规格 |
|------|------|
| SPI 信号 | SCLK, MOSI, CS |
| 控制信号 | DC, RST, BUSY |
| SPI 时钟 | ≤20 MHz |

### 2.2 按键规格

#### 电源键

| 参数 | 规格 |
|------|------|
| 按键数量 | 1 个物理按键 |
| 按键类型 | 轻触按键（微动开关） |
| 按键位置 | 设备侧边 (硬件团队决定) |
| 功能 | 开机/关机/进入屏保 |
| 触发力度 | 150-200gf |
| 模式 | 短按、长按 |
| 使用寿命 | ≥100,000 次 |

#### 旋钮按键

| 参数 | 规格 |
|------|------|
| 按键数量 | 1 个旋钮按键 |
| 按键类型 | 旋转编码器 + 按压确认 |
| 安装方式 | 垂直屏幕平面安装 |
| 按键位置 | 屏幕右下方 |
| 滚动功能 | 滚动选择列表项 |
| 按压功能 | 短按: 确认选择 / 完成任务；长按 (>1s): 返回 / 跳过任务 |
| 灵敏度 | 动态调整 (根据滚动速度) |
| 选中反馈 | 反色显示 (黑底白字) |
| 刷新方式 | 局部刷新 (快速响应) |
| 使用寿命 | ≥100,000 次 |

### 2.3 主控 SoC

| 参数 | 规格 |
|------|------|
| 芯片型号 | ESP32-S3 |
| 核心 | 双核 Xtensa LX7 |
| Flash | >= 16MB（SPI NOR Flash，模组自带） |
| PSRAM | >= 2MB，推荐 4MB |
| BLE | BLE 5.0 |
| 休眠 | 支持 Deep Sleep / 外部 GPIO 唤醒 |

Flash 用途：固件程序、OTA 分区（可选）、彩色页面缓存（>=4 页）、Event Log（环形缓冲）、屏保图片、字库

PSRAM 用途：屏幕帧缓冲、BLE 分包缓存、UI 临时数据

### 2.4 RTC（实时时钟）

| 参数 | 规格 |
|------|------|
| 功能 | 离线定时唤醒、断开 BLE 后保持计时 |
| 首选方案 | ESP32-S3 内部 RTC |
| 可选方案 | 外置高精度 RTC（如 DS3231） |

### 2.5 BLE 模块

| 参数 | 规格 |
|------|------|
| 蓝牙版本 | BLE 5.0 |
| 传输距离 | ≥10m (开阔环境) |
| 服务 UUID | `0000FFE0-0000-1000-8000-00805F9B34FB` |
| 写特征 UUID | `0000FFE1-0000-1000-8000-00805F9B34FB` |
| 通知特征 UUID | `0000FFE2-0000-1000-8000-00805F9B34FB` |

设备广播包必须包含服务 UUID `0000FFE0-0000-1000-8000-00805F9B34FB`。App 当前按 Service UUID 扫描，不按设备名扫描；如果固件只广播设备名，App 会搜不到设备。

### 2.6 电源管理

| 参数 | 4寸规格 | 7寸规格 |
|------|---------|---------|
| 电池容量 | 500mAh | 1000mAh |
| 待机时间 | ~30 天 | ~20 天 |
| 活跃使用 | ~7 天 | ~5 天 |
| 充电接口 | USB-C | USB-C |
| 充电时间 | ~1.5 小时 | ~2.5 小时 |

### 2.7 其他规格

| 参数 | 4寸规格 | 7寸规格 |
|------|---------|---------|
| 外壳材质 | ABS + PC | ABS + PC |
| 尺寸 (mm) | 95 × 75 × 8 | 175 × 115 × 10 |
| 重量 | ~60g | ~150g |
| 工作温度 | 0°C ~ 40°C | 0°C ~ 40°C |
| 存储温度 | -20°C ~ 60°C | -20°C ~ 60°C |

---

## 3. 显示页面设计

设备共有 4 个主要页面，通过时段自动切换或用户手动操作切换。

### 3.1 Page 1: Start of Day (早安页)

**显示时机:** 开机默认显示

**页面内容:**

```
┌─────────────────────────────────┐
│                                 │
│     [伴侣主形象 - 早安姿态]      │
│                                 │
│   "Good morning! Ready for      │
│    a great day?"                │
│                                 │
│   ─────────────────────────     │
│                                 │
│   3 tasks, 2 events today.      │
│                                 │
│   Next: 09:00 Team standup      │
│                                 │
│   [天气图标] 22°C Partly Cloudy │
│                                 │
└─────────────────────────────────┘
```

**触发:** 开机默认显示
**退出:** 任意按键 → Overview

**数据字段:**

Page 1 与 Page 2 同属 `DayPack(0x10)`（见 §3.2 + BLE通信协议规格文档 §4.7）。一天开始页用到：`PetDialogue`（晨安气泡，单句）、`DaySummary`（一天总结，框②，**中性**面板文案）、`FirstUp`（下一项，框③）、天气（`Weather 0x04`，含高/低温）。**旧字段 `morningGreeting / dailySummary / firstItem` 已于 v2.5.0 删除/更名，勿照其实现。**

### 3.2 Page 2: Overview (概览页)

> **重要（v2.5.0+，本节模型已更新）**：Page 1（一天开始）与 Page 2（概览）由**同一个 `DayPack(0x10)` 帧**驱动，不是各自独立的帧/字段。设备 UI **不是旧的「混合时间线」**（日程+任务交织成一条列表），而是「**常驻宠物气泡 + 可换数据面板**」：宠物口吻只有一句气泡 `PetDialogue`，其余皆为结构化面板数据（**事件卡、任务表分块，非混合**）。**权威字段与布局以 BLE通信协议规格文档 §4.7（DayPack）+ §6 / §6.5 为准**；下方旧 ASCII 示意与字段名仅作历史参考。

**面板构成（按设计稿渲染，详见 BLE §4.7 / §6.5）:**
- 顶栏: 天气（`Weather 0x04`，含高/低温）+ 日期。
- 宠物气泡: `PetDialogue`（单句，阶段感知）。
- 一天总结（框②）: `DaySummary`（**中性**面板文案，非宠物口吻；只谈日程 + 一条实用建议）。
- 下一项（框③）: `FirstUp`（下一个未来事件「HH:mm 标题」，无则置顶任务标题；App 算好下发）。
- 事件卡: `Events[]`（时间 / 标题 / 描述），按时间、只读。
- 任务表: `TopTasks[]`（标题 / 完成态 / 优先级），**按优先级排序**，可选进 Task In。
- 进度 / 能量: 来自 `SettlementData` 数值（完成 / 总数、积分、专注指标、能量瓶）。

> 「进度条 vs 任务清单」「三种情况（有日程 / 下一个日程 / 无日程）」由**固件按现有数据自行渲染**（App 两者都发，不引入 PanelMode 字节，§6.5）。「任务前三按 AI 排序（最好上手 / 最重要 / 最易获正反馈）」是**待客户确认项（暂缓）**，当前 App 仍按优先级排。

**显示时机:** 从 Start of Day 按任意键进入

**页面内容（旧示意，已被上文取代；实为事件卡 + 任务表分块）:**

```
┌─────────────────────────────────┐
│  [伴侣主形象]    Next: 14:00    │
│                 Product Review  │
│                                 │
│  ─────────────────────────────  │
│                                 │
│  混合时间线                      │
│                                 │
│  09:00 Team standup (日程)      │
│  ▶ Review project proposal      │
│  10:30 Client call (日程)       │
│  ○ Send weekly report           │
│                                 │
│  ─────────────────────────────  │
│                                 │
│  "Keep going, you're doing      │
│   great!"                       │
│                                 │
│  [滚轮滚动选择 / 按下确认]       │
└─────────────────────────────────┘
```

**排序规则:**
- 日程: 按时间排序，仅显示不可交互
- 任务: 按优先级排序，可选择进入 Task In

**显示数量:** 根据屏幕尺寸调整 (4寸/7寸不同)
**空状态:** 显示"今日无任务"提示

**交互:**
- 滚轮滚动: 选择项目 (反色高亮)
- 按下滚轮 (任务): 发送 `EnterTaskIn(0x10)`，等待 App 回发 `TaskInPage(0x11)` 后进入 Task In
- 按下滚轮 (日程): 显示日程详情 (自动超时返回)

**数据字段:** 见 `DayPack(0x10)`，BLE通信协议规格文档 §4.7（`PetDialogue` / `Events[]` / `TopTasks[]` / `SettlementData` / `DaySummary` / `FirstUp`）。**旧字段 `currentScheduleSummary / mixedTimeline / companionPhrase` 已于 v2.5.0 删除，勿照其实现。**

### 3.3 Page 3: Task In (任务详情页)

**显示时机:** 从 Overview 选择任务进入

**页面内容:**

```
┌─────────────────────────────────┐
│                                 │
│     [伴侣主形象 - 专注姿态]      │
│                                 │
│  ─────────────────────────────  │
│                                 │
│  Current Task                   │
│                                 │
│  "Review project proposal"      │
│                                 │
│  Description: Check all items   │
│  and provide feedback           │
│                                 │
│  ─────────────────────────────  │
│                                 │
│  "You can do this! Focus and    │
│   conquer!"                     │
│                                 │
│  [按下滚轮: 标记完成]            │
└─────────────────────────────────┘
```

**触发:** 从 Overview 选择任务进入
**交互:**
- 短按滚轮: 标记完成 → 自动返回 Overview
- 长按滚轮 (>1s): 跳过任务 → 返回 Overview

**事件同步:**
- 进入任务时: 发送 `EnterTaskIn` 事件 (带时间戳)
- 完成任务时: 发送 `CompleteTask` 事件 (带时间戳)
- 跳过任务时: 发送 `SkipTask` 事件 (带时间戳)

**数据字段:**

| 字段 | 最大长度 | 描述 |
|------|----------|------|
| taskTitle | 40 字符 | 任务标题 |
| taskDescription | 100 字符 | 任务描述 (可选) |
| encouragement | 50 字符 | 鼓励语 |
| focusChallengeActive | 1 byte | 专注挑战状态 |

### 3.4 Page 4: Settlement (结算页)

**显示时机:** 所有任务完成时自动显示

**页面内容:**

```
┌─────────────────────────────────┐
│                                 │
│     [伴侣主形象 - 开心姿态]      │
│                                 │
│  ─────────────────────────────  │
│                                 │
│  Today's Summary                │
│                                 │
││  Tasks: 3/5 completed           │
│  Points: +50                    │
│                                 │
│  Focus: 2h 15m (3 sessions)    │
│  Longest focus: 45 min          │
│  Interruptions: 5               │
│                                 │
│  ─────────────────────────────  │
│                                 │
│  "Great progress today!"        │
│                                 │
│  "See you tomorrow!"            │
│                                 │
└─────────────────────────────────┘
```

**触发:** 所有任务完成时自动显示
**退出:** 任意按键 → 停留在 Overview

**数据字段:**

| 字段 | 描述 |
|------|------|
| tasksCompleted | 完成任务数 |
| tasksTotal | 总任务数 |
| pointsEarned | 获得积分 |
| totalFocusMinutes | 总专注时长 (分钟) |
| focusSessionCount | 专注会话次数 |
| longestFocusMinutes | 最长单次专注时长 (分钟) |
| interruptionCount | 中断次数 (手机解锁) |
| summaryMessage | 总结语 (50 字符) |
| encouragementMessage | 鼓励语 (50 字符) |

---

### 3.5 智能提醒显示

**触发:** App 通过 BLE 推送 SmartReminder (0x13) 命令

**显示方式:** Banner 覆盖层，叠加在当前页面上方

**页面内容:**

```
┌─────────────────────────────────┐
│  ┌───────────────────────────┐  │
│  │ [伴侣小图标]               │  │
│  │                           │  │
│  │ "Time to start that       │  │
│  │  proposal review!"        │  │
│  │                           │  │
│  │ [任意按键关闭]             │  │
│  └───────────────────────────┘  │
│                                 │
│     (当前页面内容模糊显示)       │
│                                 │
└─────────────────────────────────┘
```

**提醒类型:**

| 类型 | 值 | 描述 |
|------|-----|------|
| Gentle | 0x00 | 温和提醒，普通显示 |
| Urgent | 0x01 | 紧急提醒，加粗边框 |

**交互:**
- 任意按键: 关闭提醒 banner，发送 ReminderAcknowledged (0x16) 事件
- 自动超时 (10 秒): 自动关闭，发送 ReminderDismissed (0x17) 事件
- 提醒关闭后恢复显示当前页面

**数据字段:**

| 字段 | 最大长度 | 描述 |
|------|----------|------|
| reminderText | 60 字符 | 提醒文案 |
| reminderType | 1 字节 | 提醒类型 (0x00=Gentle / 0x01=Urgent) |
| petMood | 1 字节 | 伴侣心情 (用于显示对应表情) |

---

### 3.6 屏保模式

**触发:** 按电源键
**内容:** 静态伴侣场景图（桌面装饰，满屏 1600×1200，底图**无文字**）+ **金句文本叠加层**
**金句:** 由 App 生成、经 BLE 推送的**动态文本**（如励志引文 + 作者），固件运行时叠加绘制在屏保图上（建议绘制区＝画面上半中部）。屏保底图本身不含任何文字。
- **帧已升级为业务帧（v2.5.10）**：金句经 **`0x16` Screensaver 业务帧** 下发（payload 字段 `ContentType / SceneByte / PostcardDay / QuoteLen+Quote / AuthorLen+Author`，见 BLE 通信协议规格文档 **§4.15**）。App 端已实现金句**生成**（`ScreensaverService.getScreensaverConfig` / `OpenAIService.generateScreensaverQuote`，引文 <60 字符）与**封帧下发**（`BLEDataEncoder.encodeScreensaver` → `BLEService.sendScreensaverConfig`，经 `writeData`）。
- ⚠️ **发送路径（固件须知）**：
  1. **被动触发、且依赖此刻仍连接**：App 不主动定时推送金句，而是在收到硬件 sleep 事件（`handleHardwareSleep`）后即时下发。若固件进入屏保后**立刻断连**，这帧可能来不及送达——固件应在断连前预留收帧窗口，或缓存上一次金句。
  2. ~~secure mode 下被拒~~ **已解决（v2.5.10）**：金句已从旧 `0xAA 01 02` 开发命令升级为 `0x16` 业务帧、改走 `writeData`——**dev / secure（生产签名通道）两种模式均可发**（secure 下自动经 SecureEnvelope 封装）。原「仅开发/未签名模式可达」的限制不再存在。
- ⚠️ **渲染侧（固件待实现）**：**固件需正式支持 `0x16` Screensaver 业务帧并渲染 Quote/Author**（按 BLE 协议 §4.15 的 payload 布局解析）。这是标准业务帧、不再是可忽略的 `0xAA` 开发命令。屏保帧**可能单包亦可能分包**（Quote/Author 长或 secure 封装后超 MTU），固件须按 §3.2 通用分包重组、不可假设恒为单包。
- ⚠️ **场景底图经 `0x17` SceneUnlock 切换（固件待实现）**：屏保叠加金句的**底图**（harbor/forest/nightCity）由 App 经 `0x17` SceneUnlock 业务帧推送/切换（payload = 1B SceneId：`0x00=harbor / 0x01=forest / 0x02=nightCity`，恒为单包，见 BLE 协议 §4.16）。旧 `0xAA 01 01` 场景命令已废弃；注意 `0x17` 出站=SceneUnlock、入站=ReminderDismissed（方向分离）。
**BLE:** 进入屏保后断开连接（省电）——因此金句须在**断连前**送达并由固件缓存，断连期间用缓存文本渲染。
**退出:** 再次按电源键 → Start of Day

---

## 4. 交互流程

### 4.1 页面切换方式 (3 种)

| 触发方式 | 描述 | 目标页面 |
|----------|------|----------|
| 日程触发 | 固件唤醒 + App 响应推送新数据 | Overview |
| 按钮触发 | 滚轮选择任务 → 按下进入 | Task In |
| 屏保触发 | 按电源键进入静态屏保 | 屏保 (伴侣图片) |

### 4.2 滚轮交互

#### Overview 页面

| 操作 | 响应 |
|------|------|
| 滚轮滚动 | 选择项目 (反色高亮)，纯本地处理 |
| 按下滚轮 (任务) | 进入 Task In 页面 |
| 按下滚轮 (日程) | 显示日程详情 (自动超时返回) |

#### Task In 页面

| 操作 | 响应 |
|------|------|
| 短按滚轮 | 标记任务完成，发送 CompleteTask 事件，返回 Overview |
| 长按滚轮 (>1s) | 跳过任务，发送 SkipTask 事件，返回 Overview |

#### Settlement 页面

| 操作 | 响应 |
|------|------|
| 任意按键 | 返回 Overview |

### 4.3 智能提醒交互

| 操作 | 响应 |
|------|------|
| App 推送 SmartReminder | 设备显示 banner 覆盖层 |
| 任意按键 | 关闭 banner，发送 ReminderAcknowledged 事件 |
| 10 秒无操作 | 自动关闭 banner，发送 ReminderDismissed 事件 |

### 4.4 电源键交互

| 当前状态 | 操作 | 响应 |
|----------|------|------|
| 任意页面 | 按电源键 | 进入屏保模式 |
| 屏保模式 | 按电源键 | 返回 Start of Day |
| 关机状态 | 按电源键 | 开机，显示 Start of Day |

### 4.5 状态机

```
┌─────────────┐
│  首次配对   │ ← 新设备
└──────┬──────┘
       │ App 配对成功
       ▼
┌─────────────┐  电源键  ┌─────────────┐
│ Start of Day│◄────────►│   屏保      │
└──────┬──────┘          └─────────────┘
       │ 任意按键              │
       ▼                       │ 电源键
┌─────────────┐                │
│  Overview   │◄───────────────┘
└──────┬──────┘
       │ 滚轮选择 + 按下
       ▼
┌─────────────┐  完成任务  ┌─────────────┐
│  Task In    │───────────►│ Settlement  │
└──────┬──────┘  (全部完成) └──────┬──────┘
       │ 完成单个任务              │ 任意按键
       └───────────────────────────┘
              返回 Overview
```

**状态说明:**

| 状态 | 描述 |
|------|------|
| 首次配对 | 新设备等待 App 配对 |
| Start of Day | 开机默认页面，显示早安信息 |
| Overview | 主页面，宠物气泡 + 数据面板（见 §3.2）|
| Task In | 任务详情页，专注模式 |
| Settlement | 结算页，所有任务完成时显示 |
| 屏保 | 静态伴侣图片，BLE 断开省电 |

---

## 5. 伴侣显示系统

### 5.1 伴侣角色

当前产品只有三位 IP 伴侣，设备通过 `PetStatus.CharacterId` 区分：

| CharacterId | 显示名 | 固件含义 |
|-------------|--------|----------|
| `joy` | Joy | 喜乐，轻松陪伴 |
| `silas` | Silas | 仁爱，温和稳住用户 |
| `nova` | Nova | 节制 / 自律，冷静聚焦 |

固件不得再实现旧的多形态选择系统。角色身份以 App 下发的 `CharacterId` 为准。

### 5.2 心情状态

| 心情 | 英文名 | 触发条件 |
|------|--------|----------|
| 开心 | Happy | 默认状态 |
| 兴奋 | Excited | 连续完成多个任务 |
| 专注 | Focused | 进入 Task In 页面 |
| 困倦 | Sleepy | 夜间时段 |
| 想念 | Missing You | 长时间未互动 |

### 5.3 角色图片资源需求

每个角色至少需要以下资源:

```
{characterId}/
├── main.png          # 主形象
├── head.png          # 头像 / 小图标
├── focused.png       # 专注态
├── happy.png         # 开心态
└── sleepy.png        # 休息态
```

**图片规格:**

| 规格 | 4寸屏幕 | 7寸屏幕（旧·待修订） |
|------|---------|---------|
| 尺寸 | 64 × 64 px | ~~128 × 128 px~~ |
| 格式 | 1-bit BMP | ~~1-bit BMP~~ |
| 颜色 | 黑白 | ~~黑白~~ |

> ⚠️ **7 寸伴侣图规格待修订（与实际交付不符）**：7 寸已是 1600×1200 全彩 Spectra 6 屏，本次硬件资源交付包（`docs/硬件资源/Joy/_交付硬件/`）提供的是 **Spectra6 全彩、原生约 400px、4bpp、`0x4` 作透明哨兵**的宠物精灵，而非旧表的 1-bit 黑白 128×128。待固件团队确认接受后更新本表。**4 寸规格暂保留。**

---

## 6. 数据同步

### 6.1 BLE 协议概要

第一次硬件联调请先参考 `BLE初次联调指南.md`。完整协议请参考 `BLE通信协议规格文档.md`。

BLE `Type` 字节必须按方向解释。同一个 `Type` 在 App→Device 和 Device→App 中可以有不同含义，固件不能只按 `Type` 判断业务。

#### 主要命令

| 命令 | 类型码 | 方向 | 描述 |
|------|--------|------|------|
| DayPack | 0x10 | App → Device | 发送每日数据包 |
| TaskInPage | 0x11 | App → Device | 发送任务详情 |
| DeviceMode | 0x12 | App → Device | 设置设备模式 |
| SmartReminder | 0x13 | App → Device | AI 智能提醒推送 |
| FocusStatus | 0x14 | App → Device | 推送当前专注阶段、会话能量瓶子数、已专注分钟数和任务标题 |
| EventLogRequest | 0x20 | App → Device | 请求指定时间戳之后的事件日志 |
| DeviceWake | 0x30 | Device → App | 设备唤醒并上报电量 |
| EnterTaskIn | 0x10 | Device → App | 进入任务详情，触发 App 回发 TaskInPage |
| CompleteTask | 0x11 | Device → App | 任务完成事件 |
| WheelSelect | 0x14 | Device → App | 滚轮选择确认 (发送选中项 ID；App 当前仅记录/调试，不回发详情) |
| ViewEventDetail | 0x15 | Device → App | 查看日程详情 |
| ReminderAcknowledged | 0x16 | Device → App | 用户确认收到提醒 |
| ReminderDismissed | 0x17 | Device → App | 提醒超时自动关闭 |
| RequestRefresh | 0x20 | Device → App | 请求刷新数据 |
| EventLogBatch | 0x21 | Device → App | 批量回传事件日志 |
| LowBattery | 0x40 | Device → App | 低电量通知 |

> 说明：`0x10`、`0x11`、`0x12`、`0x13`、`0x14`、`0x20`、`0x21` 都可能按方向复用。例如 App→Device 的 `0x14` 是 `FocusStatus`，Device→App 的 `0x14` 是 `WheelSelect`。固件需按链路方向区分解析。

### 6.2 事件发送时机

| 触发条件 | 发送事件 |
|----------|----------|
| 滚轮滚动 | 纯本地处理，不发送事件 |
| 按下确认并进入任务详情 | EnterTaskIn (发送 TaskId + 时间戳；App 回发 TaskInPage) |
| 普通确认但不进入任务详情 | WheelSelect (发送选中项 ID；App 当前仅记录/调试，不回发详情) |
| 按下确认 (日程) | ViewEventDetail |
| 完成任务 | CompleteTask |
| 跳过任务 | SkipTask |
| 设备唤醒 | DeviceWake (发送 BatteryLevel) |
| 用户手动刷新 | RequestRefresh |
| 用户按键关闭提醒 | ReminderAcknowledged (0x16) |
| 提醒 10 秒超时 | ReminderDismissed (0x17) |
| 电量低于 20% | LowBattery |

第一轮联调中，任务详情页可以暂不实现；如果暂不实现，不要用 `WheelSelect` 代替 `EnterTaskIn`，否则 App 不会回发 TaskInPage。

### 6.3 数据刷新时机

| 触发条件 | 刷新内容 |
|----------|----------|
| App 启动 | 完整 DayPack |
| 设备连接 | 完整 DayPack |
| 任务状态变更 | 增量更新 |
| 用户手动刷新 | 完整 DayPack |
| 定时刷新 (每小时) | 完整 DayPack |

### 6.4 初次联调范围

第一次实验室联调只验证 BLE 基础收发：

- 广播 `FFE0` Service UUID。
- 提供 `FFE1` Write 和 `FFE2` Notify。
- 发送 `DeviceWake(0x30)` 上报电量。
- 接收并打印 `Time(0x05)`。
- 发送 `RequestRefresh(0x20)`。
- 接收并重组 `DayPack(0x10)`，先打印字段，不要求完整 UI 渲染。

第一次联调暂不验收：

- 安全握手和 `SecureData(0x7E)`。
- `EventLogBatch(0x21)` 离线补传。
- `TaskInPage(0x11)` 详情页。
- `FocusStatus(0x14)` 专注实时状态。
- `SmartReminder(0x13)`。
- Spectra 6 图片帧传输。

### 6.5 离线行为

| 场景 | 设备行为 |
|------|----------|
| 未连接 App | 显示缓存数据 |
| 按键完成任务 | 本地标记 + 延迟同步 |
| 跨日未同步 | App 推送新 Day Pack |

---

## 7. 4寸 vs 7.3寸差异

### 7.1 布局适配

#### 4寸屏幕 (400 × 600)

- 紧凑布局
- 伴侣图较小 (64 × 64)
- 最多显示 3 条任务
- 单列布局

#### 7.3寸屏幕 (1600 × 1200)

> ⚠️ 以下布局参数（伴侣图尺寸、任务条数、双列）及 §7.2 字号、§7.3 信息密度原按 800×480 设定，**1600×1200 下空间是原来的 5 倍，需重新设计**。下列为旧值，仅供过渡参考。

- 宽松布局
- 伴侣图较大 (旧 128 × 128；本次交付伴侣精灵为原生约 400px，见 §5.3 与硬件资源交付包)
- 最多显示 5 条任务（旧值，1600×1200 可显著增加）
- 可选双列布局

### 7.2 字体（字库 / 角色映射 / 字号）

**内置两套字库**（已烧入固件，App **不传字体**——按字段语义角色渲染，见 BLE 协议 §6.6）：

| 字体 | 角色 | 用于 |
|------|------|------|
| **Lugrasimo**（手写 / 装饰） | 宠物口吻 | `PetDialogue` 伴侣气泡（**仅此一处**） |
| **Calibri**（无衬线 / 数据） | 中性·数据 | 顶栏天气/日期、进度/结算、事件卡（标题+描述）、`DaySummary`、`TaskInPage`、屏保 Quote/Author 等其余**所有**文本 |

> **字形回退**：`PetDialogue` 为 LLM 生成的英文短句，偶含 Lugrasimo 缺失字形（弯引号 `’ “ ”`、长破折号 `—`、emoji）时，**回退 Calibri** 渲染该字符（标准 ASCII / glyph fallback）。**不逐串传字体**：字库已烧进固件，按「字段角色 → 字体」映射即可。

**字号**（旧值；§7.1 注：1600×1200 下空间是原来 5 倍，需按新分辨率重核）：

| 元素 | 4寸 | 7寸 |
|------|-----|-----|
| 标题 | 16px | 24px |
| 正文 | 12px | 16px |
| 辅助文字 | 10px | 12px |

### 7.3 信息密度

| 内容 | 4寸 | 7寸 |
|------|-----|-----|
| 任务列表 | 3 条 | 5 条 |
| 日程预览 | 1 条 | 3 条 |
| 天气详情 | 仅图标+温度 | 完整信息 |

---

## 8. 固件开发指南

### 8.1 开发环境

| 项目 | 推荐 |
|------|------|
| MCU | ESP32-S3 模组 |
| IDE | ESP-IDF v5.x |
| E-ink 驱动 | GxEPD2 / 厂商 SDK |

### 8.2 内存布局

```
Flash (>= 16MB SPI NOR):
├── Bootloader (64KB)
├── Firmware (2MB)
├── OTA 分区 (2MB, 可选)
├── Companion Assets (3MB)
│   ├── joy/
│   ├── silas/
│   └── nova/
├── Fonts / 字库 (1MB)
├── 屏保图片 (4寸 120KB / 7寸 960KB)   ⚠️ 7寸 1600×1200 全屏屏保单张 960KB，原 512KB 不足
├── 彩色页面缓存 (>= 4 页)
│   ├── 4寸: 4 × 120KB = 480KB
│   └── 7.3寸: 4 × 960KB = 3.84MB   ⚠️ 1600×1200，需复核 Flash 余量
├── Event Log 环形缓冲 (256KB)
└── 预留 (7寸方案约 2.9MB)   ⚠️ 按实物 1600×1200：页缓存 3.84MB、屏保 960KB，预留偏紧，请确认 16MB Flash 够用
```

PSRAM 用途 (>= 2MB，推荐 4MB):
- 屏幕帧缓冲: 4寸 120,000 bytes / **7.3寸 960,000 bytes** (Spectra 6 4bpp) ⚠️ 7.3寸 1600×1200，单帧 960KB，PSRAM 余量需复核
- BLE 分包缓存 (包头 9 字节 + payload，需支持多包重组)
- UI 临时数据

### 8.3 关键实现点

#### 电源管理

```c
// 休眠唤醒
void enter_deep_sleep() {
    // 保存当前状态
    save_state_to_rtc();
    // 配置唤醒源: 按键 + 定时器
    esp_sleep_enable_ext0_wakeup(BUTTON_PIN, 0);
    esp_sleep_enable_timer_wakeup(SLEEP_DURATION_US);
    esp_deep_sleep_start();
}
```

#### E-ink 刷新策略

```c
typedef enum {
    REFRESH_FULL,      // 全刷新 - 页面切换
    REFRESH_PARTIAL,   // 局部刷新 - 任务状态更新
    REFRESH_FAST       // 快速刷新 - 动画帧
} RefreshMode;
```

#### BLE 数据接收

简单命令使用 3 字节头 (type + length)，大数据使用 9 字节分包头（详见 BLE通信协议规格文档 Section 3.2）。

```c
void on_ble_data_received(uint8_t* data, size_t len) {
    if (len < 3) {
        return;
    }

    uint8_t type = data[0];
    uint16_t payload_len = (data[1] << 8) | data[2];
    if (len != 3 + payload_len) {
        return;
    }

    uint8_t* payload = &data[3];

    switch (type) {
        case 0x10: // DayPack (分包重组后)
            parse_day_pack(payload, payload_len);
            refresh_display(REFRESH_FULL);
            break;
        case 0x11: // TaskInPage
            parse_task_in_page(payload, payload_len);
            show_task_in_page();
            break;
        case 0x13: // SmartReminder
            parse_smart_reminder(payload, payload_len);
            show_reminder_banner();
            break;
        // ...
    }
}
```

DayPack / TaskInPage 可能以 9 字节分包到达。固件应先按 `MessageId + Seq + Total` 重组，校验每包 CRC16-CCITT-FALSE，再把完整 payload 交给页面解析。当前 App 不支持分包 ACK，也不支持单包重传；固件发现缺包、CRC 错或超时时，丢弃整条消息，等待 App 后续重新发送。

### 8.4 测试清单

- [ ] BLE 连接稳定性 (连续 24 小时)
- [ ] 电池续航测试 (待机 + 活跃使用)
- [ ] E-ink 刷新质量 (残影、Spectra 6 六色显示)
- [ ] 按键响应 (防抖、长按检测)
- [ ] 数据解析 (边界条件、异常数据)
- [ ] 离线模式 (缓存、状态恢复)
- [ ] 智能提醒 banner 显示与超时关闭
- [ ] DayPack / TaskInPage 字段顺序与 App 侧 BLEDataEncoder 一致
- [ ] 专注指标在 Settlement 页面正确显示
- [ ] BLE 分包重组 (9 字节头 + CRC16 校验)
- [ ] 广播包包含 `FFE0` Service UUID，App 可扫描到设备
- [ ] `WheelSelect(0x14)` 与 `FocusStatus(0x14)` 按方向区分

---

## 附录 A: 术语表

| 术语 | 定义 |
|------|------|
| Day Pack | 每日数据包，包含 4 个页面的所有数据 |
| Event Log | 设备发送给 App 的用户交互事件 |
| Task In | 任务详情页面 |
| Settlement | 每日结算页面 |
| Focus Mode | 专注模式，简化显示 |
| Smart Reminder | AI 智能提醒 (deadline/idle/gentleNudge) |
| Companion Character | Joy / Silas / Nova 三位 IP 伴侣 |
| Focus Metrics | 专注时间、会话数、最长专注、中断次数 |

## 附录 B: 修订历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0.0 | 2026-01-30 | 初始版本 |
| v1.1.0 | 2026-01-31 | 更新按键配置 (电源键 + 滚轮)、交互流程、状态机、BLE 事件 |
| v1.1.1 | 2026-02-03 | 明确滚轮按键位置为屏幕下方 |
| v1.1.2 | 2026-02-12 | 屏幕规格更新为 Spectra 6 全彩色，7寸改为 7.3寸，4寸分辨率修正，滚轮改为旋钮，新增 SoC 规格 |
| v1.2.0 | 2026-02-12 | Message House 更新: 产品定位调整为 Unlock the Flow，新增智能提醒和专注指标，新增智能提醒显示模式，BLE 命令表增加 SmartReminder |
| v1.2.1 | 2026-02-12 | 修正 4 寸屏幕分辨率 (400x300 → 400x600)，新增关联文档索引 |
| v1.3.0 | 2026-02-13 | 修正固件开发指南 MCU (ESP32-C3 → ESP32-S3)；Flash 内存布局更新为 16MB 并对齐 Spectra 6 帧缓冲；刷新时间引用 datasheet 实测值；事件发送时机表补充 Reminder 事件；术语表补充 Message House 术语；测试清单补充新功能项 |
| v1.4.1 | 2026-05-08 | 同步 BLE 完整协议 v2.3.5：补充广播 Service UUID 要求、Type 按方向解释、第一次联调范围、WheelSelect 不触发 TaskInPage、分包无 ACK 边界 |
| v1.4.0 | 2026-05-07 | 对齐当前 App 协议：删除旧任务拆分字段，改为 Joy/Silas/Nova 伴侣和标准任务详情字段 |
| v1.5.0 | 2026-06-25 | **7.3 寸分辨率更正为 1600×1200（4:3）**（原误为 800×480）：更新 §2.1 分辨率/PPI/刷新、§8.2 帧缓冲 192K→960K 与页缓存 768K→3.84M；§5.3 伴侣图标注待修订（实际交付为 Spectra6 全彩原生约 400px，非 1-bit 128×128）；§7 布局/字号/密度标注需按新分辨率重设。**4 寸（400×600）未定，保持不动。** |
| v1.5.2 | 2026-06-29 | §3.1/§3.2 Overview 模型更新：旧「混合时间线」→ 现行「常驻气泡 + 数据面板」（PetDialogue + Events[] + TopTasks[] + SettlementData + DaySummary + FirstUp），对齐 BLE通信协议规格文档 §4.7/§6.5，旧 DayPack 字段标废弃；关联文档 BLE 协议版本 v2.3.5→v2.5.9。并含 §2.1 屏幕尺寸硬件确认更新（4寸 552×768@237DPI、7.3寸 1600×1200@282DPI、帧缓冲） |

### 关联文档

| 文档 | 版本 | 描述 |
|------|------|------|
| 硬件需求文档-Hardware-Requirements-Document.md | v0.3 | 硬件电气需求（SoC、显示、电源、电池） |
| BLE初次联调指南.md | v0.1.0 | 第一次硬件联调用，限定基础扫描、连接、收发范围 |
| BLE通信协议规格文档.md | v2.5.9 | BLE 通信协议（命令格式、数据结构、事件定义） |

---

## 联系方式

如有协议问题或需要澄清，请联系 Kirole 开发团队。
